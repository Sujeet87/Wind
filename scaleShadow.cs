using UnityEngine;
using System.Collections;

public class scaleShadow : MonoBehaviour {

	// Use this for initialization
	void Start () {
        //iTween.ValueTo(gameObject, iTween.Hash(iT.ValueTo.time,1f, iT.ValueTo.from, startValue, iT.ValueTo.to, targetSize, iT.ValueTo.onupdate, "stretch"));
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
